# DevSetup

Mon premier environnement de développement configuré de A à Z.

## Contenu du repo
- `notes/` : mes notes d'apprentissage (VSCode, Git, terminal)
- `.gitignore` : fichiers à ignorer par Git

## Outils installés
- VSCode
- Git
- Node.js

## Comment utiliser ce repo
1. Cloner le repo : `git clone <url>`
2. Ouvrir avec VSCode : `code .`